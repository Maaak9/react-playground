
const defaultState = {
  quiz: {
    list: [],
  },
};


const SpotifyQuizCreatorReducer = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default SpotifyQuizCreatorReducer;
